# Optional synthetic cloud experiments only. Not loaded automatically.
# Copy to an ignored .env file and replace the placeholder with your literal key.
# Pass that path explicitly with --env-file. Never commit the populated file.
# The offline baseline and installed local models require no cloud credential.
DEEPSEEK_API_KEY=replace-with-your-key
